import 'package:flutter/material.dart';

class ColorStyle {
  static const primaryColor = Color(0xFFFF975D);
  static const background = Color(0xFF1E2026);
  static const cardColorLight = Colors.white;
  static const cardColorDark = Color(0xFF23252E);
  static const fontColorLight = Colors.black;
  static final fontColorDark = Colors.white.withOpacity(0.9);
  static const fontSecondaryColorLight = Colors.black12;
  static const fontSecondaryColorDark = Colors.white70;
  static const iconColorLight = Colors.black;
  static const iconColorUnselecLight = Colors.black12;
  static final iconColorDark = Colors.white.withOpacity(0.9);
  static const fontColorDarkTitle = Color(0xFF32353E);
  static const grayBackground = Color(0xFF1E2026);
  static const whiteBackground = Colors.white;
  static const blackBackground = Color(0xFF1E2026);
  static const bottomBarDark = Color(0xFF1E2026);
}

class OnboardingColor {
  static const dotColor = Colors.blueAccent;
  static const activeColor = Colors.blue;
}

//text clr
const backgroundColor = Colors.black;
const kWhite = Colors.white;
const kBlack = Colors.black;
const kGrey = Colors.grey;
const kBlue = Colors.blue;
const kRed = Colors.red;
const kPrimaryColor = Color(0xFF16dec7);
final kTransp = Colors.black.withOpacity(0.5);

final kUnselectColor = Colors.black.withOpacity(0.3);
